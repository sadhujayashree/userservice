# Align load balancer probe with actuator (already permitted in SecurityConfiguration)
option_settings:
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /actuator/health
    MatcherHTTPCode: 200
